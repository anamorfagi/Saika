"""ПАМЯТЬ — один поток событий с тремя горизонтами сжатия.

Не три отдельных хранилища, а одна лента, которую время сжимает:

* RAW — последние часы дословно, как было сказано;
* эпизоды — раз в два часа сжатие с оценкой важности: что из этого
  стоило запомнить;
* образы людей — раз в неделю: кто этот человек, а не что он сказал.

Смысл разделения в том, что в промпт не влезает всё. Свежее нужно
дословно, вчерашнее — смыслом, а про человека достаточно образа.

Категории говорящих: владелец, постоянный, посторонний — они решают,
что попадает в долгую память, а что стирается вместе с горизонтом.

Реализация: `memory.py`. Настройки: `memory.*` в `config.json`.
"""
