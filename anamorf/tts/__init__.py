"""ГОЛОС — озвучка со сменными движками и приоритетом клона.

Приоритет один и тот же всегда: сначала Qwen3-TTS с клоном голоса из
`voice/ref.mp3` — это и есть голос Сайки. Всё остальное — страховка на
случай, когда клон не поднят, выгружен под нехватку памяти или упал:
Piper и Silero работают локально на процессоре, edge-tts — онлайн.

Важное свойство: озвучка идёт ПРЕДЛОЖЕНИЯМИ. Она начинает говорить, не
дожидаясь, пока модель допишет ответ, — иначе пауза перед первым словом
складывается из времени всей генерации.

Кто есть кто:

* `manager.py` — приоритет движков, стрим по предложениям, выгрузка под
                 давлением железа, самопочинка упавшего движка.
* `extra.py`   — запасные движки (Piper, Silero, edge-tts и пр.).
* `shape.py`   — темп и высота ПОВЕРХ любого движка: форма голоса
                 отделена от того, кто его синтезирует.

Настройки: `tts.*` в `config.json`, разбор — в `CONFIG.md`.
"""
