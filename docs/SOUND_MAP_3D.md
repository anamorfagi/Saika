# ТРЁХМЕРНАЯ КАРТА ЗВУКОВ — математическая модель

Просьба владельца (2026-08-16): «найди документации и проекты на гите,
построй математическую модель, 3D-карту звуков». Референс — научная
визуализация птичьего пения: точки в пространстве, соединённые нитями,
цвет по шкале с легендой.

Это ПРОЕКТ, а не отчёт: здесь модель целиком, чтобы её можно было
собрать в любой момент, в том числе в другом чате.

---

## 1. Что смотрели

| проект | что делает | что берём |
|--------|-----------|-----------|
| [Bird-Song-Visualization-in-3D](https://github.com/venomez-viper/Bird-Song-Visualization-in-3D-) | каждая точка — реальный пик БПФ, линия — мелодия | идею «точка = измеренный пик, линия = ход во времени» |
| [SoundPlot](https://github.com/naqchoalimehdi/SoundPlot-An-Open-Source-Framework-for-Birdsong-Acoustic-Analysis-and-Neural-Synthesis-) | акустический анализ + интерактивная 3D-визуализация | структуру: измерение → проекция → интерактив |
| [3D-Speaker (ModelScope)](https://github.com/modelscope/3D-Speaker) | верификация и диаризация дикторов | напоминание: пространство голосов строит ОБУЧЕННАЯ модель, а не ручные признаки |
| [Awesome-Speaker-Diarization](https://github.com/DongKeon/Awesome-Speaker-Diarization) | свод работ по диаризации | подтверждение: метки ставят ПОСЛЕ кластеризации всего окна, а не на лету |

---

## 2. Модель

Каждая фраза — одна точка. У точки шесть измеренных величин; три идут в
координаты, три в оформление. Ни одна не выдумана — все считаются из
самого звука.

### Координаты

Пространство голосов не физическое: у ECAPA 192 измерения, и «высота» в
нём смысла не имеет. Поэтому координаты получаются проекцией, а
физический смысл несут ЦВЕТ и РАЗМЕР.

```
v ∈ ℝ¹⁹²   — эмбеддинг ECAPA фразы (нормирован: ‖v‖ = 1)
V = [v₁ … vₙ]ᵀ ∈ ℝⁿˣ¹⁹²
```

Проекция в три измерения — PCA как основа (устойчива, детерминирована,
считается за миллисекунды):

```
C = (V − v̄)ᵀ(V − v̄) / (n−1)          ковариация
C uₖ = λₖ uₖ                          собственные векторы
pᵢ = [(vᵢ − v̄)·u₁, (vᵢ − v̄)·u₂, (vᵢ − v̄)·u₃]
```

`u₁, u₂, u₃` — три направления, вдоль которых голоса различаются сильнее
всего. Доля объяснённой дисперсии `λₖ / Σλ` показывается на осях: если
первые три направления держат меньше половины, картинка честно
предупреждает, что плоскость врёт.

UMAP оставляем как второй режим: он лучше разносит кластеры, но не
воспроизводим между запусками и не имеет осей с числами. Для «как в
научной работе» PCA правильнее — у него оси означают ровно то, что
написано.

### Цвет — основная частота

```
c(f₀) = (log f₀ − log 60) / (log 400 − log 60),   c ∈ [0,1]
```

Логарифм обязателен: слух воспринимает отношения частот, а не разности.
Шкала — от синего (низкий мужской, ~70 Гц) через зелёный (~150) к
красному (~350 Гц, детский или крик). Рядом колонка-легенда с делениями
в герцах: это и есть «попадание по частотам», которое просил владелец.

Точки без измеренного тона (шёпот, шипящие) — серые. Врать серым честнее,
чем красить наугад.

### Размер — уверенность

```
r(d, snr) = r₀ · (0.6 + 0.4 · min(1, d / 2.0)) · (0.5 + 0.5 · min(1, snr / 20))
```

Длинная и громкая фраза — крупная точка: её отпечаток надёжен. Обрывок в
полсекунды на грани шума — мелкая: видно, что ей доверять нельзя.

### Нити — ход во времени

Точки одного человека соединяются в порядке произнесения:

```
E = { (pᵢ, pⱼ) : who(i) = who(j),  j = следующая фраза этого голоса,
                 tⱼ − tᵢ < 60 с }
```

Прозрачность нити падает с разрывом во времени. Так видно не облако, а
ПУТЬ: как голос гуляет по пространству тембров, пока человек говорит.
Именно это делает референс с птичьим пением читаемым.

### Плотность — где человек живёт

Для каждой карточки голоса считается эллипсоид рассеяния:

```
μ = среднее pᵢ этого голоса
Σ = ковариация pᵢ
оболочка: (p − μ)ᵀ Σ⁻¹ (p − μ) = χ²(0.9, 3) ≈ 6.25
```

Полупрозрачный эллипсоид вместо выпуклой оболочки: он не растягивается
на один выброс и честно показывает разброс, а не крайние точки.

---

## 3. Что нужно доделать в коде

Отмечаю по местам, чтобы можно было продолжить с любого места.

1. **Тон на точку.** `server/voiceprint/registry.py` хранит `pts`,
   `pts_who`, `pts_ts`. Нужен четвёртый массив `pts_hz` — значение уже
   считается в `_process` и сейчас выбрасывается. Без него цветовой шкалы
   не будет (старые точки останутся серыми, и это правильно).
2. **Третья координата.** `server/voiceprint/projector.py` проецирует в
   2D. Добавить режим `dims=3` и отдавать долю объяснённой дисперсии.
3. **Отдача наружу.** `points()` в `server/voiceprint/__init__.py` должен
   вернуть `z`, `hz`, `sec`, `snr` на точку.
4. **Отрисовка.** В `ui/index.html` карта рисуется на 2D-канве. Для
   объёма — поворот сцены матрицей и сортировка точек по глубине (ничего
   тяжелее не нужно, WebGL тут избыточен):

   ```
   p' = Rᵧ(θ) Rₓ(φ) p
   экран: (x', y') · f/(f − z'),   f — фокус
   рисовать по убыванию z': дальние сначала
   ```

   Мышь тянет θ и φ, колесо — масштаб. Дальние точки бледнее (туман по
   глубине) — единственный дешёвый признак объёма, который читается.
5. **Легенда.** Колонка-градиент слева, деления 70 / 100 / 150 / 220 /
   330 Гц, подпись «основная частота».

---

## 4. Чего эта карта НЕ покажет

Честности ради, чтобы потом не спорить с картинкой:

- расстояние на экране ≈ различие голосов, но не пропорционально ему:
  проекция из 192 измерений в 3 всегда что-то сминает;
- два голоса рядом не значит «один человек» — значит «ECAPA не видит
  между ними разницы на этих кусках»;
- у не-голосовых звуков (стук, клавиатура, музыка) в этом пространстве
  места нет вообще: оно построено на модели ГОЛОСОВ. Для них своя карта —
  `server/sound_map.py` на эмбеддингах PANNs, и смешивать их нельзя.
